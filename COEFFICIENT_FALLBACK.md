# 🔄 Автоматический поиск ближайших коэффициентов

## Что изменилось?

Теперь система **не выдает ошибку**, если категория ткани не найдена точно. Вместо этого используется **умный поиск** с несколькими уровнями fallback.

## Как это работает?

### 1️⃣ Поиск системы (systemKey)

```
Запрос: "UNI1_ZEBRA" → Найдено: "uni1_zebra" ✅
```

- Ищет точное совпадение
- Если не найдено — ищет без учета регистра и пробелов
- Если система вообще не найдена — показывает ошибку ❌

### 2️⃣ Поиск категории ткани

```
Запрос: "e" → Найдено: "E" ✅
Запрос: " 1 " → Найдено: "1" ✅
Запрос: "XYZ" → Найдено: "1" (первая доступная) ⚠️
```

- Ищет точное совпадение
- Если не найдено — ищет без учета регистра
- Если не найдено — убирает пробелы и ищет снова
- **Если категория не найдена вообще** — автоматически использует **первую доступную категорию** для этой системы

### 3️⃣ Поиск по размерам

```
Размер: 1.37м × 1.83м → Коэффициент: 137.4991 (интерполирован) ✅
```

- Использует **билинейную интерполяцию**
- Автоматически находит значение между ближайшими 4 точками
- Работает для любых размеров в пределах доступного диапазона

## Примеры

### ✅ Успешные случаи

| Запрошено           | Найдено           | Результат                           |
| ------------------- | ----------------- | ----------------------------------- |
| `uni1_zebra`, `E`   | `uni1_zebra`, `E` | Точное совпадение                   |
| `UNI1_ZEBRA`, `E`   | `uni1_zebra`, `E` | Нормализация регистра               |
| `uni1_zebra`, `e`   | `uni1_zebra`, `E` | Нормализация регистра категории     |
| `uni1_zebra`, `XYZ` | `uni1_zebra`, `1` | **Fallback на первую категорию** ⚠️ |

### ❌ Ошибка

| Запрошено             | Результат                 |
| --------------------- | ------------------------- |
| `unknown_system`, `E` | **Система не найдена** ❌ |

## Что увидит пользователь?

### При использовании fallback категории:

![image](https://img.shields.io/badge/Информация-Использована%20ближайшая%20категория-blue)

```
💡 Использована ближайшая категория
Категория "XYZ" не найдена, использована "1". Коэффициент рассчитан корректно.
```

- Заказ **создается успешно** ✅
- Коэффициент рассчитан
- Показывается информационное уведомление
- Створка **не подсвечивается красным**

### Если система не найдена:

![image](https://img.shields.io/badge/Ошибка-Система%20не%20найдена-red)

```
❌ Система не найдена
Система "my_system" не найдена в файле коэффициентов.
Проверьте настройку system_key в справочнике систем.
```

- Заказ **не создается** ❌
- Створка **подсвечивается оранжевым**
- Показывается значок предупреждения ⚠️

## Команды для проверки

### Посмотреть все системы:

```bash
npm run check:coefficients
```

### Посмотреть категории для системы:

```bash
npm run check:coefficients uni1_zebra
```

### Тестирование fallback логики:

```bash
npx tsx server/test-coefficients.ts
```

## Преимущества новой логики

✅ **Не требует точного совпадения** категорий  
✅ **Автоматически находит ближайшую** категорию  
✅ **Не блокирует работу** из-за опечаток в категориях  
✅ **Интерполирует размеры** автоматически  
✅ **Логирует предупреждения** в консоль сервера  
✅ **Информирует пользователя** о fallback

## Рекомендации

1. **Используйте точные категории** — fallback это запасной вариант, не основной
2. **Проверяйте system_key** — он должен точно совпадать с ключом в coefficients.json
3. **Следите за предупреждениями** — если часто видите fallback, исправьте категории в справочнике тканей

## Логи в консоли сервера

```
[Coefficients] Найдена категория "E" вместо "e" (без учета регистра)
[Coefficients] Рассчитан коэффициент 139.6500 для системы "uni1_zebra",
               категория "E" (запрошена: "e"), размер 1.5×2м
[Coefficients] Категория "XYZ" не найдена. Используется fallback: "1".
               Доступные категории: 1, 2, 3, 4, E
```

Все операции логируются для диагностики и отладки.


